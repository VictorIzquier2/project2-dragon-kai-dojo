<div class="container-classes">
  <h1 style="{{hide}}">First Round</h1>
  <form action="/classes/tourney/firstRound" method="POST" id="firstRound">
    <div class="main-classes">
      <div class="list">
        {{#each vencedores}}
            <div class="entry ranking">
            <input type="checkbox" id="{{_id}}" name="_id" checked value="{{_id}}" onchange="countPlayersFRSelected(this)">
            <img src="{{imageUrl}}" alt="{{name}}">
            <label for="{{_id}}">{{name}}</label>
            <img src="/images/{{level}}.jpg" alt="{{level}}">
            <h2>💚 {{mana}}</h2>
          </div>
        {{/each}}
        <button class="btn btn-firstRound" onclick="enableFRBtn()" style="{{hide}}">Second Round</button>
      </div>
    </div>
  </form>
</div>

