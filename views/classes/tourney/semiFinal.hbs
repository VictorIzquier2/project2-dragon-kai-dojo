<div class="container-classes">
  <h1 style="{{hide}}">Semi Final</h1>
  <form action="/classes/tourney/semiFinal" method="POST" id="semiFinal">
    <div class="main-classes">
      <div class="list">
        {{#each vencedores}}
            <div class="entry ranking">
            <input type="checkbox" id="{{_id}}" name="_id" checked value="{{_id}}" onchange="countPlayersSFSelected(this)">
            <img src="{{imageUrl}}" alt="{{name}}">
            <label for="{{_id}}">{{name}}</label>
            <img src="/images/{{level}}.jpg" alt="{{level}}">
            <h2>💚 {{mana}}</h2>
          </div>
        {{/each}}
        <button class="btn btn-semiFinal" onclick="enableSFBtn()" style="{{hide}}">Final</button>
      </div>
    </div>
  </form>
</div>